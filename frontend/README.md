# Frontend

Run standalone with `npm install && npm run dev`. The app starts in offline mode using synthetic records from `../contracts/sample-data`; its role-aware client is ready to switch to the backend with `VITE_API_URL`.
