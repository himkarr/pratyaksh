# Demo script

1. Start with `docker compose up --build`; log in as `ministry_demo@aqua.test` / `demo1234` and state that all records are synthetic, review signals—not accusations.
2. Use the demo role switcher to show MP, State Nodal, District, and Ministry data scopes.
3. Open flags from each origin: `rule`, `isolation_forest`, and `deadline_forecaster`; explain the visible reason and confidence.
4. Point out the deadline badge for a project with a predictive breach probability before its mandatory one-year completion date.
5. Honest limitation: show an ambiguous synthetic record; its borderline progress and spend trajectory should prompt review rather than a claim of wrongdoing.
6. Live constraint-break: after showing **Verify integrity** passes, alter an `audit_events.this_hash` value in the database. Verify again and show the reported broken event ID. Restore the seed database afterward.
