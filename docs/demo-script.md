# Demo script

1. State that all displayed records are synthetic and the system supports human review, never accusations.
2. Switch between the four roles and show that each sees only its permitted scope.
3. Open an explainable flag and point out its severity, origin tag, confidence, and plain-language reason.
4. Show a predictive deadline-risk badge for a low-progress project before its one-year deadline.
5. Live constraint-break moment: change a synthetic project's utilization above its sanctioned amount and call `/predict`; the named cost-overrun rule supplies an explanation.
6. Show the Ministry audit endpoint and explain its append-only hash chain.
